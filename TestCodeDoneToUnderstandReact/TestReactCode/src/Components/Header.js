import React from "react"

function Header(){
	//Can only apply className to react elements and not Components such as "header"
	return(
		<header className="navbar">This is the header</header>
	)
}

export default Header
